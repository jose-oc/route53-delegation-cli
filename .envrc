watch_file pyproject.toml
watch_file .envrc

export AWS_PROFILE=tfc
export UV_PROJECT_ENVIRONMENT="$PWD/.venv"
export UV_CACHE_DIR="$PWD/.uv-cache"

if [[ -d .venv/bin ]]; then
  PATH_add .venv/bin
  export VIRTUAL_ENV="$PWD/.venv"
fi
